# 通义千问 API（LLM 调用）
# 推荐通过前端首页输入框配置 API Key，此处留空即可
# 留空时 Agent 走纯规则模式，在前端输入 Key 后自动启用 LLM 增强
DASHSCOPE_API_KEY=

# 是否启用 LLM（false 时纯规则运行，前端传入 Key 会自动覆盖为 true）
USE_LLM=true

# LLM 基础配置
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen-plus
